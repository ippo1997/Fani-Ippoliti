//
// Created by alpacox on 05/03/21.
//

#include "precompiler.h"
#include "Dice.h"

/*
Dice::Dice(int f): faces(f){
    srand(time(0));
}

int Dice::roll(int r){

    int result = 0;

    for (int i = 0; i < r ; i++)
        result += rand() % faces + 1;
    return result;
}
 */